# from .state import GameState
# from pecas.peca import Cor


# def carregar_fen(fen: str, engine_instancia) -> GameState:
#     state = GameState()
#     partes = fen.strip().split()
#     # ... (mesma lógica que você já tem, mas salvando em 'state')
#     # Use engine_instancia.criar_peca se precisar da fábrica
#     return state


# def exportar_fen(state: GameState) -> str:
#     # ... (mesma lógica usando os atributos de 'state')
#     return fen_string
